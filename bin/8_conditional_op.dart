void main() {
  var a = 4;
  var b = 3;

  var smallerNum = a < b ? a :b;

      a < b ? print ('$a is smaller') :
  print('$b is smaller');

      print(smallerNum);


      var name;

      // will be null if theres no value ( var v1 = name;
  // will be not null if theres a value ( var v1 = name ?? 'willy';
      var v1 = name;

      print(v1);

}